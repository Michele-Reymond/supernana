<p>


<% if (((Time.now - alert.started_at)/60).to_i) <= 60 %>
  Alerte créée il y a <%= (((Time.now - alert.started_at)/60).to_i) == 1 ? "#{((Time.now - alert.started_at)/60).to_i} minute" : "#{((Time.now - alert.started_at)/60).to_i} minutes" %>
<% else %>
  Alerte créée il y a plus d'une heure
<% end %>

par <strong><%= alert.user.first_name %> <%= alert.user.last_name %></strong>

<% if alert.ended_at? %>
  <% if (((Time.now - alert.ended_at)/60).to_i) <= 60 %>
    et arrêtée il y a <%= (((Time.now - alert.ended_at)/60).to_i) == 1 ? "#{((Time.now - alert.ended_at)/60).to_i} minute" : "#{((Time.now - alert.ended_at)/60).to_i} minutes" %>
  <% else %>
    et arrêtée il y a plus d'une heure.
  <% end %>
<% else %>
  et elle est toujours active!!!
<% end %>


</p>
