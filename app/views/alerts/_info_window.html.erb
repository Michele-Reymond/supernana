<p>Alerte déclenchée par <%= alert.user.first_name %> <%= alert.user.last_name %></p>

<% if (((Time.now - alert.started_at)/60).to_i) <= 60 %>
  <p> Crée il y a <%= (((Time.now - alert.started_at)/60).to_i) == 1 ? "#{((Time.now - alert.started_at)/60).to_i} minute" : "#{((Time.now - alert.started_at)/60).to_i} minutes." %></p>
<% else %>
  <p>Crée il y a plus d'une heure. </p>
<% end %>


<% if alert.ended_at? %>
  <% if (((Time.now - alert.ended_at)/60).to_i) <= 60 %>
    <p>Arrêtée il y a <%= (((Time.now - alert.ended_at)/60).to_i) == 1 ? "#{((Time.now - alert.ended_at)/60).to_i} minute" : "#{((Time.now - alert.ended_at)/60).to_i} minutes." %></p>
  <% else %>
    <p>Arrêtée il y a plus d'une heure. </p>
  <% end %>
<% else %>
  <p>Cette alerte est toujours active!!!</p>
<% end %>

