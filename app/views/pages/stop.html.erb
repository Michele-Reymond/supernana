<div class="s-container">
  <%= link_to "Désactiver l'alarme", alert_path(@alert), method: :patch %>

  <p>Pas de panique...</p>
  <% unless current_user.contacts.first == nil %>
    <p>Nous avons envoyé un message à <%= @contact.first_name %></p>
  <% end %>
  <p>Les secours ont été contactés</p>
  <p>Vous êtes géolocalisée près de <%= @alert.address %></p> <!-- géolocalisation à faire -->

  <%= audio_tag "alarm.mp3", autoplay: true %>
</div>

